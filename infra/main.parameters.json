{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": { "value": "${AZURE_ENV_NAME=demo}" },
    "location": { "value": "${AZURE_LOCATION=eastus2}" },
    "azdEnvName": { "value": "${AZURE_ENV_NAME=demo}" },
    "postgresAdminUser": { "value": "pgadmin" },
    "postgresAdminPassword": { "value": "${POSTGRES_ADMIN_PASSWORD}" },
    "aoaiEndpoint": { "value": "${AOAI_ENDPOINT}" },
    "aoaiApiKey": { "value": "${AOAI_API_KEY}" }
  }
}
