name: recruiter-agent
metadata:
  template: recruiter-agent@0.1.0

services:
  backend:
    project: ./backend
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
  frontend:
    project: ./frontend
    language: ts
    host: staticwebapp
    dist: out
